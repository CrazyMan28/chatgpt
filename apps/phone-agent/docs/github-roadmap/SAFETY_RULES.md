# Safety Rules

- Require explicit approval for risky device, file, or shell operations.
- Default to deny when the intent, target, or permission is ambiguous.
- Keep compound-flow step boundaries visible so users can stop or review them.
- Never suppress a failure that should surface to the user or issue log.
